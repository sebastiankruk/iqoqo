# activitypub-federation-core Specification

## Purpose
Define the ActivityPub federation protocol rules, actor endpoints, and outbox/inbox behaviors for decentralized iqoqo instances.

## Requirements
