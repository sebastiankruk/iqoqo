# Copyright (C) 2026 Sebastian Ryszard Kruk (dev@kruk.me)
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published
# by the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>
#
# =============================================================================
# myKG — Knowledge Graph Extractor
# =============================================================================
# ACTIVE PROFILE — change this one line to switch provider and all settings.
# Available: ollama-local | anthropic-claude | openai | gemini | openrouter-free |
#            claude-cli | agent-claude-code
# =============================================================================
profile: agent-claude-code

# ---------------------------------------------------------------------------
# Profiles — each is fully self-contained: provider, credentials, and pipeline.
# Set `profile:` above to activate one.
# Or override per run without editing this file:
#   mykg extract-graph <dir> --profile <name> [--model <model>]
# ---------------------------------------------------------------------------
profiles:

# ---------------------------------------------------------------------------
  openrouter-free:
  # OpenRouter free tier — conservative settings for 32K-context free models.
  # Change llm.model to target a specific model (openrouter.ai/models).
  # Requires OPENROUTER_API_KEY in env / .env.mykg
  #
  # Token budget chain (use context-calculator to recompute when switching models):
  #   ① context_window    = 128000
  #   ② max_output_tokens = 32000
  #   ③ input_headroom    = ① − ② = 96000
  #      batch_token_target = 32000  (manually reduced further from 64000 to shrink per-batch blast radius on failure)
  #      window_tokens      = batch_token_target / 5 = 6400
  #      overlap_tokens     = window_tokens × 0.10 = 640
  # ---------------------------------------------------------------------------
    provider: openrouter
    llm_retry:
      max_retries: 1        # retries on empty/blank response; set to 0 to disable
    llm:
      context_window: 128000     # ① model's total context limit — root anchor for all budget calculations below
      max_output_tokens: 32000   # ② output cap — google/gemma-4-31b-it supports far more; raised from free-tier default
      # If a chunk response hits the output cap, the JSON is truncated mid-stream causing parse errors.
      # Reduce window_tokens so the model has less to say per chunk, or raise max_output_tokens if the model supports it.
      base_url: https://openrouter.ai/api/v1
      # Set model to a specific OpenRouter model slug (see openrouter.ai/models).
      # For 128K+ models raise window_tokens, overlap_tokens, and batch_token_target proportionally
      # (run: context-calculator --context <N> --max-output <M>).
      model: openrouter/free
      timeout: 1800                # seconds per LLM call before raising a timeout error
      retry_429_max: 1          # max retries on HTTP 429 rate-limit before giving up
      retry_429_base_delay: 2.0 # initial backoff seconds; doubles on each retry (exponential)
    pipeline:
      # --- Ingestion & chunking -----------------------------------------------
      ingest:
        max_workers: 8          # parallel file readers; safe to set high — purely I/O-bound
      # --- Preprocess (optional non-md document conversion via MinerU) ----------
      # MinerU runs inside an ephemeral uv-managed venv created per parse-docs
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      preprocess:
        enabled: true                # set true to convert non-md files (PDF/DOCX/images) via MinerU before ingest
        subdir: _preprocessed         # converted .md files land in <session>/input/<subdir>/
        keep_artifacts: false        # when false (default) keep only the final <stem>.md; drop MinerU's nested subtree, images/, and .mineru.json sidecar. Set true to retain everything MinerU wrote (debugging).
        extra_args: []                # extra arguments passed through to mineru (e.g. ["--backend", "pipeline"])
        timeout_seconds: 1800         # mineru run-phase timeout (30 min)
        uv_path: uv                   # path or PATH name of the uv CLI (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter version uv pins the ephemeral venv to
        mineru_spec: "mineru[all] pdftext<0.7.0"      # package spec installed via `uv pip install -U <spec>`
        install_timeout_seconds: 1800 # install-phase timeout (30 min); first-run downloads PyTorch/Ray
        extensions:                   # suffixes the preprocess step may convert (.md is native and never listed). Backend per suffix is hardcoded: HTML → markdownify in-process, everything else → MinerU. Remove a line to skip that suffix.
          - .pdf
          - .docx
          - .doc
          - .pptx
          - .xlsx
          - .png
          - .jpg
          - .jpeg
          - .html
          - .htm
          - .txt
      # --- Fetch (optional web crawl via fetch-web command) --------------------
      # The crawler runs inside an ephemeral uv-managed venv created per fetch-web
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      fetch:
        enabled: true                 # master toggle for the fetch-web command
        output_dir: mykg_web_fetch    # default base folder for fetch-web output (./<output_dir>/<domain>/); overridden by --output
        strategy: same-domain         # link-following scope, passed to Crawlee's enqueue_links(strategy=...):
                                       #   same-domain — follow links to the same registrable domain, across subdomains (e.g. docs.example.com + www.example.com)
                                       #   same-origin — follow links matching scheme+host+port exactly (stricter than same-domain; subdomains excluded)
                                       #   all         — follow any link found, including off-site domains (use with care: max_pages/max_depth become the only guardrails)
        max_pages: 1000                # hard cap on total fetched pages (bounds runtime)
        max_depth: 1                   # max crawl depth from the seed URL
        respect_robots: true          # honor robots.txt disallow + crawl-delay
        request_delay_seconds: 0.5    # politeness delay between requests
        concurrency: 4                # max concurrent requests
        download_assets: true         # download linked binaries whose suffix is in preprocess.extensions
        timeout_seconds: 1800         # crawl run-phase timeout (30 min)
        uv_path: uv                   # uv CLI path (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter pinned for the ephemeral crawl venv
        crawlee_spec: "crawlee[beautifulsoup]"   # spec installed via `uv pip install -U`
        install_timeout_seconds: 1800 # install-phase timeout (30 min)
        github_clone_enabled: true    # master toggle for the GitHub-repo git-clone path (vs Crawlee)
        github_clone_depth: 1          # `git clone --depth N` — shallow clone (history depth only; full working tree is always cloned). 1 = latest commit's files only, no history. Increase (or set higher) only if you want .git/ history in _repo/ for provenance.
        github_clone_timeout_seconds: 1800  # git clone timeout (30 min)
        max_workers: 2                 # max concurrent seed crawls inside one shared venv for --url-list
      chunking:
        window_tokens: 6400      # = batch_token_target / 5
        overlap_tokens: 640      # = window_tokens × 0.10
        tiktoken_encoding: cl100k_base  # tokenizer used for token counting (matches most OpenAI-family models)
      # --- Pass 1: schema induction -------------------------------------------
      pass1:
        batch_token_target: 32000  # MUST stay ≤ context_window − max_output_tokens
        max_workers: 8
        per_file_batching: false  # set true for Option B: one batch per source file
        max_schema_proposals: 25  # cap raw proposals sent to harmonize_schema; prevents context overflow on large corpora
        random_seed: 0  # fixed seed for deterministic batch sampling when batches exceed max_schema_proposals; scoped per-call, not shared across repeated Pass 1 invocations
      # --- Pass 2: instance extraction ----------------------------------------
      pass2:
        max_workers: 8
        stateful_chunks: true   # pass prior-chunk nodes to next chunk so IDs stay stable across chunk boundaries
        prep_mode: batch_chunks                # per_file | concat | batch_chunks — controls how files are prepared before Pass 2
        per_file_token_target: 32000   # token cap per file; used when prep_mode=per_file. Files over this are truncated to the first N tokens. 0 = no cap
        concat_batch_token_target: 32000  # target token budget per virtual batch; used when prep_mode=concat
        batch_token_target: 32000          # token budget per chunk batch; used when prep_mode=batch_chunks
        batch_per_file: false              # when true, never mix chunks from different files in one batch; used when prep_mode=batch_chunks
        batch_retry_max: 1                 # max in-run retry rounds for failed batches (0 = disabled)
      normalize_names:
        enabled: true           # run the name-normalization LLM step to resolve surface-form variants before deduplication
        max_names_per_type: 50000 # safety-net cap per type; primary batch sizing is controlled by batch_token_target
        batch_token_target: 32000 # max tokens per LLM normalization call; types are bin-packed greedily up to this limit
      # --- Orphan recovery -----------------------------------------------------
      orphan_pass:
        enabled: false
        min_cooccurrence: 1     # minimum times an orphan and a connected node must appear in the same chunk to be a candidate
        top_k_per_orphan: 5     # max candidate connected nodes considered per orphan in Stage 1 scoring
        confidence_base: 0.5    # starting confidence for an inferred orphan edge before weighting
        confidence_weight: 0.5  # multiplier applied to co-occurrence frequency when computing final orphan edge confidence
        max_workers: 4
        schema_max_restarts: 0  # max times the pipeline may restart Pass 2 after orphan_connect proposes new schema properties
        excerpt_window: 4000     # character window around an orphan mention used as LLM context in Stage 2
        excerpt_context: 1500    # characters of surrounding context added before/after the excerpt_window
        excerpt_max_total: 16000 # hard cap on total characters sent to the LLM per orphan group prompt
        blank_recovery_enabled: true  # attempt to recover nodes from chunks that returned blank LLM responses in Pass 2
        connected_sample_size: 200     # max number of already-connected nodes included in the Stage 2 orphan prompt
      # --- Assembly & deduplication -------------------------------------------
      assembly:
        confidence_agg: mean    # how to combine confidence scores when merging duplicate nodes/edges: mean | max
        edge_id_prefix: "edge-"
        edge_id_hex_length: 6   # hex digits appended to edge_id_prefix to form unique edge IDs (e.g. edge-3f9a1c)
        edge_dedup_separator: "|"  # separator used when building the dedup hash key: "<type>|<from_id>|<to_id>"
        confidence_fallback: 0.0   # confidence assigned when the LLM provides no score for an attribute
        confidence_scalar_omitted: 0.5  # confidence assigned when the LLM returns a scalar value instead of {value, confidence}
      # --- Export --------------------------------------------------------------
      export:
        schema_namespace: "http://mykg.local/schema/"  # RDF namespace for RDFS classes and properties (TBox)
        data_namespace: "http://mykg.local/data/"      # RDF namespace for individual node instances (ABox)
        rdf_namespace: "http://www.w3.org/1999/02/22-rdf-syntax-ns#"   # standard RDF namespace (rdf:type, rdf:Property)
        rdfs_namespace: "http://www.w3.org/2000/01/rdf-schema#"        # standard RDFS namespace (rdfs:Class, rdfs:subClassOf, rdfs:domain, rdfs:range)
        schema_prefix_label: ex    # @prefix label for schema_namespace in Turtle output (e.g. ex:Person)
        data_prefix_label: data    # @prefix label for data_namespace in Turtle output (e.g. data:alice)
        comment_width: 42          # column width for section divider comments in knowledge_graph.ttl
        skos_namespace: "http://www.w3.org/2004/02/skos/core#"  # SKOS namespace for skos:altLabel alias triples
        networkx_enabled: true     # write NetworkX multi-format exports to output/networkx_output/ (GML, GraphML, GEXF, etc.)
        obsidian_enabled: true     # write Obsidian vault to output/obsidian_vault/ (markdown notes + graph links)
        obsidian_vault_dir: obsidian_vault  # subdirectory under output/ for the Obsidian vault
        neo4j_csv_enabled: false    # write Neo4j LOAD CSV bundle to output/neo4j_csv/ (CSVs + browser/cypher-shell scripts + README)
        neo4j_csv_dir: neo4j_csv    # subdirectory under output/ for the Neo4j LOAD CSV bundle
      # --- Pipeline infrastructure --------------------------------------------
      error_gate:               # circuit-breaker: pauses the run when too many API errors accumulate
        enabled: false          # set false for fully unattended runs (e.g. providers with no rate limits)
        threshold: 3            # pause and prompt for Enter after this many accumulated API errors (429s, timeouts, empty responses)
      feedback:
        max_file_chars: 224640   # max characters of schema.json included in a feedback correction prompt
      paths:
        output_dir: output
        intermediate_dir: intermediate
        sessions_dir: mykg_sessions  # parent folder for per-run session directories (each run gets a timestamped subfolder)
      output:
        json_indent: 2          # pretty-print indent for all intermediate JSON files
      logging:
        max_bytes: 10485760     # 10 MB per log file before rotation
        backup_count: 3         # number of rotated log files to keep (e.g. run.log, run.log.1, run.log.2)
        llm_log: false           # write per-call accounting (provider, model, tokens, latency, raw response) to llm.log
        capture_prompts: false   # write each LLM system+user prompt to intermediate/llm_calls/ for debugging
        error_output_max_chars: 500  # max chars of stderr/stdout included in LLM backend error messages
      report:
        enabled: true           # generate walkthrough.md after each extract run; set false to skip
      merge_graphs:
        reextraction_strategy: surgical  # none | surgical | full — controls re-extraction when merged schema has new properties absent from a source session
        surgical_top_k_chunks_per_property: 5  # 0 = no cap; >0 = top-K chunks per new property (ranked by domain/range node co-occurrence)
        human_review: false             # pause for human review of merged schema; overridden by --human-review CLI flag
        orphan_pass_max_restarts: 1     # max schema-gap restart loops during merge orphan pass (0 = disabled)
      append:
        grow_schema_backfill_top_k_chunks_per_type: 10  # --append --grow-schema: 0 = disable back-fill; >0 = top-K old chunks re-extracted per newly added concept/property (ranked by domain/range/hierarchy node co-occurrence) (D52)
    # --- MCP server — mykg mcp-serve settings --------------------------------
    mcp:
      enabled: true                # master toggle for the MCP server (mykg mcp-serve) (mykg mcp-serve)
      host: localhost              # bind address for streamable HTTP (ignored for stdio)
      port: 3100                   # port for streamable HTTP (ignored for stdio)
      transport: stdio             # default transport: stdio | streamable_http

  # ---------------------------------------------------------------------------
  anthropic-claude:
  # Anthropic Claude — direct API, requires ANTHROPIC_API_KEY in env / .env.mykg
  # (ANTHROPIC_AUTH_TOKEN is also accepted). Recommended for quality.
  # Optional: set llm.base_url for Anthropic-compatible endpoints (e.g. Bedrock proxies).
  #
  # Token budget chain (use context-calculator to recompute when switching models):
  #   ① context_window    = 200000
  #   ② max_output_tokens = 32000
  #   ③ input_headroom    = ① − ② = 168000
  #      batch_token_target = 32000  (conservative for reliable JSON output)
  #      window_tokens      = batch_token_target / 4 = 8000
  #      overlap_tokens     = window_tokens × 0.10 = 800
  # ---------------------------------------------------------------------------
    provider: anthropic
    llm_retry:
      max_retries: 2        # retries on empty/blank response; set to 0 to disable
    llm:
      context_window: 200000      # ① model's total context limit
      max_output_tokens: 32000     # ② output cap — Sonnet/Opus default max
      model: claude-sonnet-4-5
      timeout: 1800                # seconds per LLM call before raising a timeout error
      retry_429_max: 5            # max retries on HTTP 429 rate-limit before giving up
      retry_429_base_delay: 2.0   # initial backoff seconds; doubles on each retry (exponential)
      # base_url:                 # optional: override for Anthropic-compatible endpoint
      # api_key:                  # optional: override ANTHROPIC_API_KEY env var
    pipeline:
      # --- Ingestion & chunking -----------------------------------------------
      ingest:
        max_workers: 8
      # --- Preprocess (optional non-md document conversion via MinerU) ----------
      # MinerU runs inside an ephemeral uv-managed venv created per parse-docs
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      preprocess:
        enabled: true                # set true to convert non-md files (PDF/DOCX/images) via MinerU before ingest
        subdir: _preprocessed         # converted .md files land in <session>/input/<subdir>/
        keep_artifacts: false        # when false (default) keep only the final <stem>.md; drop MinerU's nested subtree, images/, and .mineru.json sidecar. Set true to retain everything MinerU wrote (debugging).
        extra_args: []                # extra arguments passed through to mineru (e.g. ["--backend", "pipeline"])
        timeout_seconds: 1800         # mineru run-phase timeout (30 min)
        uv_path: uv                   # path or PATH name of the uv CLI (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter version uv pins the ephemeral venv to
        mineru_spec: "mineru[all] pdftext<0.7.0"      # package spec installed via `uv pip install -U <spec>`
        install_timeout_seconds: 1800 # install-phase timeout (30 min); first-run downloads PyTorch/Ray
        extensions:                   # suffixes the preprocess step may convert (.md is native and never listed). Backend per suffix is hardcoded: HTML → markdownify in-process, everything else → MinerU. Remove a line to skip that suffix.
          - .pdf
          - .docx
          - .doc
          - .pptx
          - .xlsx
          - .png
          - .jpg
          - .jpeg
          - .html
          - .htm
          - .txt
      # --- Fetch (optional web crawl via fetch-web command) --------------------
      # The crawler runs inside an ephemeral uv-managed venv created per fetch-web
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      fetch:
        enabled: true                 # master toggle for the fetch-web command
        output_dir: mykg_web_fetch    # default base folder for fetch-web output (./<output_dir>/<domain>/); overridden by --output
        strategy: same-domain         # link-following scope, passed to Crawlee's enqueue_links(strategy=...):
                                       #   same-domain — follow links to the same registrable domain, across subdomains (e.g. docs.example.com + www.example.com)
                                       #   same-origin — follow links matching scheme+host+port exactly (stricter than same-domain; subdomains excluded)
                                       #   all         — follow any link found, including off-site domains (use with care: max_pages/max_depth become the only guardrails)
        max_pages: 1000                # hard cap on total fetched pages (bounds runtime)
        max_depth: 1                   # max crawl depth from the seed URL
        respect_robots: true          # honor robots.txt disallow + crawl-delay
        request_delay_seconds: 0.5    # politeness delay between requests
        concurrency: 4                # max concurrent requests
        download_assets: true         # download linked binaries whose suffix is in preprocess.extensions
        timeout_seconds: 1800         # crawl run-phase timeout (30 min)
        uv_path: uv                   # uv CLI path (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter pinned for the ephemeral crawl venv
        crawlee_spec: "crawlee[beautifulsoup]"   # spec installed via `uv pip install -U`
        install_timeout_seconds: 1800 # install-phase timeout (30 min)
        github_clone_enabled: true    # master toggle for the GitHub-repo git-clone path (vs Crawlee)
        github_clone_depth: 1          # `git clone --depth N` — shallow clone (history depth only; full working tree is always cloned). 1 = latest commit's files only, no history. Increase (or set higher) only if you want .git/ history in _repo/ for provenance.
        github_clone_timeout_seconds: 1800  # git clone timeout (30 min)
        max_workers: 2                 # max concurrent seed crawls inside one shared venv for --url-list
      chunking:
        window_tokens: 8000     # = batch_token_target / 4
        overlap_tokens: 800     # = window_tokens × 0.10
        tiktoken_encoding: cl100k_base
      # --- Pass 1: schema induction -------------------------------------------
      pass1:
        batch_token_target: 32000  # conservative; MUST stay ≤ context_window − max_output_tokens
        max_workers: 4
        per_file_batching: false
        max_schema_proposals: 25  # cap raw proposals sent to harmonize_schema; prevents context overflow on large corpora
        random_seed: 0  # fixed seed for deterministic batch sampling when batches exceed max_schema_proposals; scoped per-call, not shared across repeated Pass 1 invocations
      # --- Pass 2: instance extraction ----------------------------------------
      pass2:
        max_workers: 4
        stateful_chunks: true
        prep_mode: batch_chunks                # per_file | concat | batch_chunks — controls how files are prepared before Pass 2
        per_file_token_target: 32000   # token cap per file; used when prep_mode=per_file. Files over this are truncated to the first N tokens. 0 = no cap
        concat_batch_token_target: 32000  # target token budget per virtual batch; used when prep_mode=concat
        batch_token_target: 32000         # token budget per chunk batch; used when prep_mode=batch_chunks
        batch_per_file: false              # when true, never mix chunks from different files in one batch; used when prep_mode=batch_chunks
        batch_retry_max: 1                 # max in-run retry rounds for failed batches (0 = disabled)
      normalize_names:
        enabled: true
        max_names_per_type: 50000 # safety-net cap per type; primary batch sizing is controlled by batch_token_target
        batch_token_target: 32000 # max tokens per LLM normalization call; types are bin-packed greedily up to this limit
      # --- Orphan recovery -----------------------------------------------------
      orphan_pass:
        enabled: false
        min_cooccurrence: 1
        top_k_per_orphan: 5
        confidence_base: 0.5
        confidence_weight: 0.5
        max_workers: 4
        schema_max_restarts: 0
        excerpt_window: 4000
        excerpt_context: 1500
        excerpt_max_total: 16000
        blank_recovery_enabled: true
        connected_sample_size: 200
      # --- Assembly & deduplication -------------------------------------------
      assembly:
        confidence_agg: mean
        edge_id_prefix: "edge-"
        edge_id_hex_length: 6
        edge_dedup_separator: "|"
        confidence_fallback: 0.0
        confidence_scalar_omitted: 0.5
      # --- Export --------------------------------------------------------------
      export:
        schema_namespace: "http://mykg.local/schema/"
        data_namespace: "http://mykg.local/data/"
        rdf_namespace: "http://www.w3.org/1999/02/22-rdf-syntax-ns#"
        rdfs_namespace: "http://www.w3.org/2000/01/rdf-schema#"
        schema_prefix_label: ex
        data_prefix_label: data
        comment_width: 42
        skos_namespace: "http://www.w3.org/2004/02/skos/core#"
        networkx_enabled: true
        obsidian_enabled: true
        obsidian_vault_dir: obsidian_vault
        neo4j_csv_enabled: false
        neo4j_csv_dir: neo4j_csv
      # --- Pipeline infrastructure --------------------------------------------
      error_gate:
        enabled: false
        threshold: 3
      feedback:
        max_file_chars: 224640
      paths:
        output_dir: output
        intermediate_dir: intermediate
        sessions_dir: mykg_sessions
      output:
        json_indent: 2
      logging:
        max_bytes: 10485760
        backup_count: 3
        llm_log: false
        capture_prompts: false
        error_output_max_chars: 500
      report:
        enabled: true           # generate walkthrough.md after each extract run; set false to skip
      merge_graphs:
        reextraction_strategy: surgical  # none | surgical | full — controls re-extraction when merged schema has new properties absent from a source session
        surgical_top_k_chunks_per_property: 5  # 0 = no cap; >0 = top-K chunks per new property (ranked by domain/range node co-occurrence)
        human_review: false             # pause for human review of merged schema; overridden by --human-review CLI flag
        orphan_pass_max_restarts: 1     # max schema-gap restart loops during merge orphan pass (0 = disabled)
      append:
        grow_schema_backfill_top_k_chunks_per_type: 10  # --append --grow-schema: 0 = disable back-fill; >0 = top-K old chunks re-extracted per newly added concept/property (ranked by domain/range/hierarchy node co-occurrence) (D52)
    # --- MCP server — mykg mcp-serve settings --------------------------------
    mcp:
      enabled: true                # master toggle for the MCP server (mykg mcp-serve) (mykg mcp-serve)
      host: localhost              # bind address for streamable HTTP (ignored for stdio)
      port: 3100                   # port for streamable HTTP (ignored for stdio)
      transport: stdio             # default transport: stdio | streamable_http

  # ---------------------------------------------------------------------------
  claude-cli:
  # Claude Code CLI — uses locally-installed `claude -p` subprocess.
  # No ANTHROPIC_API_KEY needed — billing is via your Claude Pro/Max plan.
  # max_workers must be 1; the claude CLI is serial by design.
  #
  # Token budget chain (use context-calculator to recompute when switching models):
  #   ① context_window    = 200000
  #   ② max_output_tokens = 32000
  #   ③ input_headroom    = ① − ② = 168000
  #      batch_token_target = 32000  (conservative for reliable JSON output)
  #      window_tokens      = batch_token_target / 4 = 8000
  #      overlap_tokens     = window_tokens × 0.10 = 800
  # ---------------------------------------------------------------------------
    provider: claude-cli
    llm_retry:
      max_retries: 2
    llm:
      model: sonnet            # passed as --model to claude -p; "auto" = let CLI pick (no flag sent)
      effort: medium            # passed as --effort to claude -p; "auto" = let CLI pick (no flag sent)
      context_window: 200000    # ① model's total context limit — root anchor for all budget calculations below
      max_output_tokens: 32000  # ② output cap; input_headroom = context_window − this = 100000
      timeout: 1800             # seconds per claude -p subprocess call
      retry_429_max: 0          # claude-cli does not hit API rate limits
      retry_429_base_delay: 2.0
    pipeline:
      # --- Ingestion & chunking -----------------------------------------------
      ingest:
        max_workers: 2          # parallel file readers; safe to set high — purely I/O-bound
      # --- Preprocess (optional non-md document conversion via MinerU) ----------
      # MinerU runs inside an ephemeral uv-managed venv created per parse-docs
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      preprocess:
        enabled: true                # set true to convert non-md files (PDF/DOCX/images) via MinerU before ingest
        subdir: _preprocessed         # converted .md files land in <session>/input/<subdir>/
        keep_artifacts: false        # when false (default) keep only the final <stem>.md; drop MinerU's nested subtree, images/, and .mineru.json sidecar. Set true to retain everything MinerU wrote (debugging).
        extra_args: []                # extra arguments passed through to mineru (e.g. ["--backend", "pipeline"])
        timeout_seconds: 1800         # mineru run-phase timeout (30 min)
        uv_path: uv                   # path or PATH name of the uv CLI (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter version uv pins the ephemeral venv to
        mineru_spec: "mineru[all] pdftext<0.7.0"      # package spec installed via `uv pip install -U <spec>`
        install_timeout_seconds: 1800 # install-phase timeout (30 min); first-run downloads PyTorch/Ray
        extensions:                   # suffixes the preprocess step may convert (.md is native and never listed). Backend per suffix is hardcoded: HTML → markdownify in-process, everything else → MinerU. Remove a line to skip that suffix.
          - .pdf
          - .docx
          - .doc
          - .pptx
          - .xlsx
          - .png
          - .jpg
          - .jpeg
          - .html
          - .htm
          - .txt
      # --- Fetch (optional web crawl via fetch-web command) --------------------
      # The crawler runs inside an ephemeral uv-managed venv created per fetch-web
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      fetch:
        enabled: true                 # master toggle for the fetch-web command
        output_dir: mykg_web_fetch    # default base folder for fetch-web output (./<output_dir>/<domain>/); overridden by --output
        strategy: same-domain         # link-following scope, passed to Crawlee's enqueue_links(strategy=...):
                                       #   same-domain — follow links to the same registrable domain, across subdomains (e.g. docs.example.com + www.example.com)
                                       #   same-origin — follow links matching scheme+host+port exactly (stricter than same-domain; subdomains excluded)
                                       #   all         — follow any link found, including off-site domains (use with care: max_pages/max_depth become the only guardrails)
        max_pages: 1000                # hard cap on total fetched pages (bounds runtime)
        max_depth: 1                   # max crawl depth from the seed URL
        respect_robots: true          # honor robots.txt disallow + crawl-delay
        request_delay_seconds: 0.5    # politeness delay between requests
        concurrency: 4                # max concurrent requests
        download_assets: true         # download linked binaries whose suffix is in preprocess.extensions
        timeout_seconds: 1800         # crawl run-phase timeout (30 min)
        uv_path: uv                   # uv CLI path (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter pinned for the ephemeral crawl venv
        crawlee_spec: "crawlee[beautifulsoup]"   # spec installed via `uv pip install -U`
        install_timeout_seconds: 1800 # install-phase timeout (30 min)
        github_clone_enabled: true    # master toggle for the GitHub-repo git-clone path (vs Crawlee)
        github_clone_depth: 1          # `git clone --depth N` — shallow clone (history depth only; full working tree is always cloned). 1 = latest commit's files only, no history. Increase (or set higher) only if you want .git/ history in _repo/ for provenance.
        github_clone_timeout_seconds: 1800  # git clone timeout (30 min)
        max_workers: 2                 # max concurrent seed crawls inside one shared venv for --url-list
      chunking:
        window_tokens: 8000     # = batch_token_target / 4
        overlap_tokens: 800     # = window_tokens × 0.10
        tiktoken_encoding: cl100k_base  # tokenizer used for token counting (matches most OpenAI-family models)
      # --- Pass 1: schema induction -------------------------------------------
      pass1:
        batch_token_target: 32000  # conservative; MUST stay ≤ context_window − max_output_tokens
        max_workers: 1          # claude-cli is serial — 1 worker only
        per_file_batching: false  # set true for Option B: one batch per source file
        max_schema_proposals: 25  # cap raw proposals sent to harmonize_schema; prevents context overflow on large corpora
        random_seed: 0  # fixed seed for deterministic batch sampling when batches exceed max_schema_proposals; scoped per-call, not shared across repeated Pass 1 invocations
      # --- Pass 2: instance extraction ----------------------------------------
      pass2:
        max_workers: 1          # claude-cli is serial — 1 worker only
        stateful_chunks: true   # pass prior-chunk nodes to next chunk so IDs stay stable across chunk boundaries
        prep_mode: batch_chunks                # per_file | concat | batch_chunks — controls how files are prepared before Pass 2
        per_file_token_target: 32000   # token cap per file; used when prep_mode=per_file. Files over this are truncated to the first N tokens. 0 = no cap
        concat_batch_token_target: 32000  # target token budget per virtual batch; used when prep_mode=concat
        batch_token_target: 32000         # token budget per chunk batch; used when prep_mode=batch_chunks
        batch_per_file: false              # when true, never mix chunks from different files in one batch; used when prep_mode=batch_chunks
        batch_retry_max: 1                 # max in-run retry rounds for failed batches (0 = disabled)
      normalize_names:
        enabled: true           # run the name-normalization LLM step to resolve surface-form variants before deduplication
        max_names_per_type: 50000 # safety-net cap per type; primary batch sizing is controlled by batch_token_target
        batch_token_target: 32000 # max tokens per LLM normalization call; types are bin-packed greedily up to this limit
      # --- Orphan recovery -----------------------------------------------------
      orphan_pass:
        enabled: false
        min_cooccurrence: 1     # minimum times an orphan and a connected node must appear in the same chunk to be a candidate
        top_k_per_orphan: 5     # max candidate connected nodes considered per orphan in Stage 1 scoring
        confidence_base: 0.5    # starting confidence for an inferred orphan edge before weighting
        confidence_weight: 0.5  # multiplier applied to co-occurrence frequency when computing final orphan edge confidence
        max_workers: 1          # claude-cli is serial — 1 worker only
        schema_max_restarts: 0  # max times the pipeline may restart Pass 2 after orphan_connect proposes new schema properties
        excerpt_window: 4000     # character window around an orphan mention used as LLM context in Stage 2
        excerpt_context: 1500    # characters of surrounding context added before/after the excerpt_window
        excerpt_max_total: 1600 # hard cap on total characters sent to the LLM per orphan group prompt
        blank_recovery_enabled: true  # attempt to recover nodes from chunks that returned blank LLM responses in Pass 2
        connected_sample_size: 200     # max number of already-connected nodes included in the Stage 2 orphan prompt
      # --- Assembly & deduplication -------------------------------------------
      assembly:
        confidence_agg: mean    # how to combine confidence scores when merging duplicate nodes/edges: mean | max
        edge_id_prefix: "edge-"
        edge_id_hex_length: 6   # hex digits appended to edge_id_prefix to form unique edge IDs (e.g. edge-3f9a1c)
        edge_dedup_separator: "|"  # separator used when building the dedup hash key: "<type>|<from_id>|<to_id>"
        confidence_fallback: 0.0   # confidence assigned when the LLM provides no score for an attribute
        confidence_scalar_omitted: 0.5  # confidence assigned when the LLM returns a scalar value instead of {value, confidence}
      # --- Export --------------------------------------------------------------
      export:
        schema_namespace: "http://mykg.local/schema/"  # RDF namespace for RDFS classes and properties (TBox)
        data_namespace: "http://mykg.local/data/"      # RDF namespace for individual node instances (ABox)
        rdf_namespace: "http://www.w3.org/1999/02/22-rdf-syntax-ns#"   # standard RDF namespace (rdf:type, rdf:Property)
        rdfs_namespace: "http://www.w3.org/2000/01/rdf-schema#"        # standard RDFS namespace (rdfs:Class, rdfs:subClassOf, rdfs:domain, rdfs:range)
        schema_prefix_label: ex    # @prefix label for schema_namespace in Turtle output (e.g. ex:Person)
        data_prefix_label: data    # @prefix label for data_namespace in Turtle output (e.g. data:alice)
        comment_width: 42          # column width for section divider comments in knowledge_graph.ttl
        skos_namespace: "http://www.w3.org/2004/02/skos/core#"  # SKOS namespace for skos:altLabel alias triples
        networkx_enabled: true     # write NetworkX multi-format exports to output/networkx_output/ (GML, GraphML, GEXF, etc.)
        obsidian_enabled: true     # write Obsidian vault to output/obsidian_vault/ (markdown notes + graph links)
        obsidian_vault_dir: obsidian_vault  # subdirectory under output/ for the Obsidian vault
        neo4j_csv_enabled: false    # write Neo4j LOAD CSV bundle to output/neo4j_csv/ (CSVs + browser/cypher-shell scripts + README)
        neo4j_csv_dir: neo4j_csv    # subdirectory under output/ for the Neo4j LOAD CSV bundle
      # --- Pipeline infrastructure --------------------------------------------
      error_gate:               # circuit-breaker: pauses the run when too many API errors accumulate
        enabled: false
        threshold: 0            # pause and prompt for Enter after this many accumulated API errors (0 = disabled)
      feedback:
        max_file_chars: 6400   # max characters of schema.json included in a feedback correction prompt
      paths:
        output_dir: output
        intermediate_dir: intermediate
        sessions_dir: mykg_sessions  # parent folder for per-run session directories (each run gets a timestamped subfolder)
      output:
        json_indent: 2          # pretty-print indent for all intermediate JSON files
      logging:
        max_bytes: 10485760     # 10 MB per log file before rotation
        backup_count: 3         # number of rotated log files to keep (e.g. run.log, run.log.1, run.log.2)
        llm_log: false           # write per-call accounting (provider, model, tokens, latency, raw response) to llm.log
        capture_prompts: false   # write each LLM system+user prompt to intermediate/llm_calls/ for debugging
        error_output_max_chars: 500  # max chars of stderr/stdout included in LLM backend error messages
      report:
        enabled: true           # generate walkthrough.md after each extract run; set false to skip
      merge_graphs:
        reextraction_strategy: surgical  # none | surgical | full — controls re-extraction when merged schema has new properties absent from a source session
        surgical_top_k_chunks_per_property: 5  # 0 = no cap; >0 = top-K chunks per new property (ranked by domain/range node co-occurrence)
        human_review: false             # pause for human review of merged schema; overridden by --human-review CLI flag
        orphan_pass_max_restarts: 1     # max schema-gap restart loops during merge orphan pass (0 = disabled)
      append:
        grow_schema_backfill_top_k_chunks_per_type: 10  # --append --grow-schema: 0 = disable back-fill; >0 = top-K old chunks re-extracted per newly added concept/property (ranked by domain/range/hierarchy node co-occurrence) (D52)
    # --- MCP server — mykg mcp-serve settings --------------------------------
    mcp:
      enabled: true                # master toggle for the MCP server (mykg mcp-serve) (mykg mcp-serve)
      host: localhost              # bind address for streamable HTTP (ignored for stdio)
      port: 3100                   # port for streamable HTTP (ignored for stdio)
      transport: stdio             # default transport: stdio | streamable_http

  # ---------------------------------------------------------------------------
  openai:
  # OpenAI — direct API, requires OPENAI_API_KEY in env / .env.mykg
  # Optional: set llm.base_url to use Azure OpenAI or any OpenAI-compatible endpoint.
  #
  # The adapter auto-routes `max_output_tokens` to the right OpenAI parameter
  # based on the configured model:
  #   • Legacy (gpt-4o, gpt-4-turbo, gpt-3.5)            → max_tokens
  #   • New families (gpt-5*, gpt-4.1*, o1/o3/o4*, chatgpt-*) → max_completion_tokens
  # If an unknown model rejects max_tokens at runtime, the adapter swaps to
  # max_completion_tokens automatically (one-shot per adapter) and logs a warning.
  #
  # Token budget chain (use context-calculator to recompute when switching models):
  #   ① context_window    = 128000
  #   ② max_output_tokens = 32000
  #   ③ input_headroom    = ① − ② = 96000
  #      batch_token_target = 32000  (conservative for reliable JSON output)
  #      window_tokens      = batch_token_target / 4 = 8000
  #      overlap_tokens     = window_tokens × 0.10 = 800
  # ---------------------------------------------------------------------------
    provider: openai
    llm_retry:
      max_retries: 2        # retries on empty/blank response; set to 0 to disable
    llm:
      context_window: 128000      # ① model's total context limit
      max_output_tokens: 32000    # ② output cap — gpt-4o max structured output
      model: gpt-5.4-mini-2026-03-17
      timeout: 1800                # seconds per LLM call before raising a timeout error
      retry_429_max: 5            # max retries on HTTP 429 rate-limit before giving up
      retry_429_base_delay: 2.0   # initial backoff seconds; doubles on each retry (exponential)
      # base_url:                 # optional: override for Azure OpenAI or compatible endpoint
      # api_key:                  # optional: override OPENAI_API_KEY env var
    pipeline:
      # --- Ingestion & chunking -----------------------------------------------
      ingest:
        max_workers: 8
      # --- Preprocess (optional non-md document conversion via MinerU) ----------
      # MinerU runs inside an ephemeral uv-managed venv created per parse-docs
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      preprocess:
        enabled: true                # set true to convert non-md files (PDF/DOCX/images) via MinerU before ingest
        subdir: _preprocessed         # converted .md files land in <session>/input/<subdir>/
        keep_artifacts: false        # when false (default) keep only the final <stem>.md; drop MinerU's nested subtree, images/, and .mineru.json sidecar. Set true to retain everything MinerU wrote (debugging).
        extra_args: []                # extra arguments passed through to mineru (e.g. ["--backend", "pipeline"])
        timeout_seconds: 1800         # mineru run-phase timeout (30 min)
        uv_path: uv                   # path or PATH name of the uv CLI (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter version uv pins the ephemeral venv to
        mineru_spec: "mineru[all] pdftext<0.7.0"      # package spec installed via `uv pip install -U <spec>`
        install_timeout_seconds: 1800 # install-phase timeout (30 min); first-run downloads PyTorch/Ray
        extensions:                   # suffixes the preprocess step may convert (.md is native and never listed). Backend per suffix is hardcoded: HTML → markdownify in-process, everything else → MinerU. Remove a line to skip that suffix.
          - .pdf
          - .docx
          - .doc
          - .pptx
          - .xlsx
          - .png
          - .jpg
          - .jpeg
          - .html
          - .htm
          - .txt
      # --- Fetch (optional web crawl via fetch-web command) --------------------
      # The crawler runs inside an ephemeral uv-managed venv created per fetch-web
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      fetch:
        enabled: true                 # master toggle for the fetch-web command
        output_dir: mykg_web_fetch    # default base folder for fetch-web output (./<output_dir>/<domain>/); overridden by --output
        strategy: same-domain         # link-following scope, passed to Crawlee's enqueue_links(strategy=...):
                                       #   same-domain — follow links to the same registrable domain, across subdomains (e.g. docs.example.com + www.example.com)
                                       #   same-origin — follow links matching scheme+host+port exactly (stricter than same-domain; subdomains excluded)
                                       #   all         — follow any link found, including off-site domains (use with care: max_pages/max_depth become the only guardrails)
        max_pages: 1000                # hard cap on total fetched pages (bounds runtime)
        max_depth: 1                   # max crawl depth from the seed URL
        respect_robots: true          # honor robots.txt disallow + crawl-delay
        request_delay_seconds: 0.5    # politeness delay between requests
        concurrency: 4                # max concurrent requests
        download_assets: true         # download linked binaries whose suffix is in preprocess.extensions
        timeout_seconds: 1800         # crawl run-phase timeout (30 min)
        uv_path: uv                   # uv CLI path (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter pinned for the ephemeral crawl venv
        crawlee_spec: "crawlee[beautifulsoup]"   # spec installed via `uv pip install -U`
        install_timeout_seconds: 1800 # install-phase timeout (30 min)
        github_clone_enabled: true    # master toggle for the GitHub-repo git-clone path (vs Crawlee)
        github_clone_depth: 1          # `git clone --depth N` — shallow clone (history depth only; full working tree is always cloned). 1 = latest commit's files only, no history. Increase (or set higher) only if you want .git/ history in _repo/ for provenance.
        github_clone_timeout_seconds: 1800  # git clone timeout (30 min)
        max_workers: 2                 # max concurrent seed crawls inside one shared venv for --url-list
      chunking:
        window_tokens: 8000     # = batch_token_target / 4
        overlap_tokens: 800     # = window_tokens × 0.10
        tiktoken_encoding: cl100k_base
      # --- Pass 1: schema induction -------------------------------------------
      pass1:
        batch_token_target: 32000  # conservative; MUST stay ≤ context_window − max_output_tokens
        max_workers: 4
        per_file_batching: false
        max_schema_proposals: 25  # cap raw proposals sent to harmonize_schema; prevents context overflow on large corpora
        random_seed: 0  # fixed seed for deterministic batch sampling when batches exceed max_schema_proposals; scoped per-call, not shared across repeated Pass 1 invocations
      # --- Pass 2: instance extraction ----------------------------------------
      pass2:
        max_workers: 4
        stateful_chunks: true
        prep_mode: batch_chunks             # per_file | concat | batch_chunks — controls how files are prepared before Pass 2
        per_file_token_target: 32000   # token cap per file; used when prep_mode=per_file. Files over this are truncated to the first N tokens. 0 = no cap
        concat_batch_token_target: 32000  # target token budget per virtual batch; used when prep_mode=concat
        batch_token_target: 32000         # token budget per chunk batch; used when prep_mode=batch_chunks
        batch_per_file: false              # when true, never mix chunks from different files in one batch; used when prep_mode=batch_chunks
        batch_retry_max: 1                 # max in-run retry rounds for failed batches (0 = disabled)
      normalize_names:
        enabled: true
        max_names_per_type: 50000 # safety-net cap per type; primary batch sizing is controlled by batch_token_target
        batch_token_target: 32000 # max tokens per LLM normalization call; types are bin-packed greedily up to this limit
      # --- Orphan recovery -----------------------------------------------------
      orphan_pass:
        enabled: false
        min_cooccurrence: 1
        top_k_per_orphan: 5
        confidence_base: 0.5
        confidence_weight: 0.5
        max_workers: 4
        schema_max_restarts: 0
        excerpt_window: 4000
        excerpt_context: 1500
        excerpt_max_total: 16000
        blank_recovery_enabled: true
        connected_sample_size: 200
      # --- Assembly & deduplication -------------------------------------------
      assembly:
        confidence_agg: mean
        edge_id_prefix: "edge-"
        edge_id_hex_length: 6
        edge_dedup_separator: "|"
        confidence_fallback: 0.0
        confidence_scalar_omitted: 0.5
      # --- Export --------------------------------------------------------------
      export:
        schema_namespace: "http://mykg.local/schema/"
        data_namespace: "http://mykg.local/data/"
        rdf_namespace: "http://www.w3.org/1999/02/22-rdf-syntax-ns#"
        rdfs_namespace: "http://www.w3.org/2000/01/rdf-schema#"
        schema_prefix_label: ex
        data_prefix_label: data
        comment_width: 42
        skos_namespace: "http://www.w3.org/2004/02/skos/core#"
        networkx_enabled: true
        obsidian_enabled: true
        obsidian_vault_dir: obsidian_vault
        neo4j_csv_enabled: false
        neo4j_csv_dir: neo4j_csv
      # --- Pipeline infrastructure --------------------------------------------
      error_gate:
        enabled: false
        threshold: 3
      feedback:
        max_file_chars: 224640
      paths:
        output_dir: output
        intermediate_dir: intermediate
        sessions_dir: mykg_sessions
      output:
        json_indent: 2
      logging:
        max_bytes: 10485760
        backup_count: 3
        llm_log: false
        capture_prompts: false
        error_output_max_chars: 500
      report:
        enabled: true           # generate walkthrough.md after each extract run; set false to skip
      merge_graphs:
        reextraction_strategy: surgical  # none | surgical | full — controls re-extraction when merged schema has new properties absent from a source session
        surgical_top_k_chunks_per_property: 0  # 0 = no cap; >0 = top-K chunks per new property (ranked by domain/range node co-occurrence)
        human_review: false             # pause for human review of merged schema; overridden by --human-review CLI flag
        orphan_pass_max_restarts: 1     # max schema-gap restart loops during merge orphan pass (0 = disabled)
      append:
        grow_schema_backfill_top_k_chunks_per_type: 10  # --append --grow-schema: 0 = disable back-fill; >0 = top-K old chunks re-extracted per newly added concept/property (ranked by domain/range/hierarchy node co-occurrence) (D52)
    # --- MCP server — mykg mcp-serve settings --------------------------------
    mcp:
      enabled: true                # master toggle for the MCP server (mykg mcp-serve) (mykg mcp-serve)
      host: localhost              # bind address for streamable HTTP (ignored for stdio)
      port: 3100                   # port for streamable HTTP (ignored for stdio)
      transport: stdio             # default transport: stdio | streamable_http

  # ---------------------------------------------------------------------------
  gemini:
  # Google Gemini — native google-genai SDK, requires GEMINI_API_KEY (or
  # GOOGLE_API_KEY) in env / .env.mykg.
  #
  # Implicit context caching is automatic and server-side on Gemini 2.5+ models:
  # a stable prompt prefix (the flattened schema in system_instruction) is cached
  # by Google with no cache object to create and no storage cost. The adapter
  # only *reports* the saving via cache_read_tokens in llm.log — it implements no
  # caching of its own.
  #
  # Gemini 3.x draws thinking tokens from the SAME max_output_tokens allowance as
  # the visible answer, so a budget that is too small returns an EMPTY response
  # with finish_reason=MAX_TOKENS. Keep max_output_tokens generous.
  #
  # The reasoning budget defaults to "low" in the adapter. To change it, add an
  # `llm.thinking_level` key here (low | medium | high, or null to let the model
  # decide). Use thinking_level, not thinking_budget — thinking_budget=0 is
  # rejected outright by gemini-3.6-flash.
  #
  # NOTE ON RATE LIMITS (Invariant 13): max_workers below matches the openai
  # profile. The Gemini FREE tier allows only 5 requests/minute/model and will
  # return 429 RESOURCE_EXHAUSTED at these settings — drop pass1/pass2/
  # orphan_pass max_workers to 1–2 if you are on it. Paid tiers handle these
  # values fine.
  #
  # Token budget chain (use context-calculator to recompute when switching models):
  #   ① context_window    = 128000
  #   ② max_output_tokens = 32000
  #   ③ input_headroom    = ① − ② = 96000
  #      batch_token_target = 32000  (conservative for reliable JSON output)
  #      window_tokens      = batch_token_target / 4 = 8000
  #      overlap_tokens     = window_tokens × 0.10 = 800
  # ---------------------------------------------------------------------------
    provider: gemini
    llm_retry:
      max_retries: 2        # retries on empty/blank response; set to 0 to disable
    llm:
      context_window: 128000      # ① model's total context limit
      max_output_tokens: 32000    # ② output cap — shared by thinking + answer
      model: gemini-3.7-flash
      timeout: 1800                # seconds per LLM call before raising a timeout error
      retry_429_max: 5            # max retries on HTTP 429 rate-limit before giving up
      retry_429_base_delay: 2.0   # initial backoff seconds; doubles on each retry (exponential)
      # api_key:                  # optional: override GEMINI_API_KEY env var
    pipeline:
      # --- Ingestion & chunking -----------------------------------------------
      ingest:
        max_workers: 8
      # --- Preprocess (optional non-md document conversion via MinerU) ----------
      # MinerU runs inside an ephemeral uv-managed venv created per parse-docs
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      preprocess:
        enabled: true                # set true to convert non-md files (PDF/DOCX/images) via MinerU before ingest
        subdir: _preprocessed         # converted .md files land in <session>/input/<subdir>/
        keep_artifacts: false        # when false (default) keep only the final <stem>.md; drop MinerU's nested subtree, images/, and .mineru.json sidecar. Set true to retain everything MinerU wrote (debugging).
        extra_args: []                # extra arguments passed through to mineru (e.g. ["--backend", "pipeline"])
        timeout_seconds: 1800         # mineru run-phase timeout (30 min)
        uv_path: uv                   # path or PATH name of the uv CLI (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter version uv pins the ephemeral venv to
        mineru_spec: "mineru[all] pdftext<0.7.0"      # package spec installed via `uv pip install -U <spec>`
        install_timeout_seconds: 1800 # install-phase timeout (30 min); first-run downloads PyTorch/Ray
        extensions:                   # suffixes the preprocess step may convert (.md is native and never listed). Backend per suffix is hardcoded: HTML → markdownify in-process, everything else → MinerU. Remove a line to skip that suffix.
          - .pdf
          - .docx
          - .doc
          - .pptx
          - .xlsx
          - .png
          - .jpg
          - .jpeg
          - .html
          - .htm
          - .txt
      # --- Fetch (optional web crawl via fetch-web command) --------------------
      # The crawler runs inside an ephemeral uv-managed venv created per fetch-web
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      fetch:
        enabled: true                 # master toggle for the fetch-web command
        output_dir: mykg_web_fetch    # default base folder for fetch-web output (./<output_dir>/<domain>/); overridden by --output
        strategy: same-domain         # link-following scope, passed to Crawlee's enqueue_links(strategy=...):
                                       #   same-domain — follow links to the same registrable domain, across subdomains (e.g. docs.example.com + www.example.com)
                                       #   same-origin — follow links matching scheme+host+port exactly (stricter than same-domain; subdomains excluded)
                                       #   all         — follow any link found, including off-site domains (use with care: max_pages/max_depth become the only guardrails)
        max_pages: 1000                # hard cap on total fetched pages (bounds runtime)
        max_depth: 1                   # max crawl depth from the seed URL
        respect_robots: true          # honor robots.txt disallow + crawl-delay
        request_delay_seconds: 0.5    # politeness delay between requests
        concurrency: 4                # max concurrent requests
        download_assets: true         # download linked binaries whose suffix is in preprocess.extensions
        timeout_seconds: 1800         # crawl run-phase timeout (30 min)
        uv_path: uv                   # uv CLI path (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter pinned for the ephemeral crawl venv
        crawlee_spec: "crawlee[beautifulsoup]"   # spec installed via `uv pip install -U`
        install_timeout_seconds: 1800 # install-phase timeout (30 min)
        github_clone_enabled: true    # master toggle for the GitHub-repo git-clone path (vs Crawlee)
        github_clone_depth: 1          # `git clone --depth N` — shallow clone (history depth only; full working tree is always cloned). 1 = latest commit's files only, no history. Increase (or set higher) only if you want .git/ history in _repo/ for provenance.
        github_clone_timeout_seconds: 1800  # git clone timeout (30 min)
        max_workers: 2                 # max concurrent seed crawls inside one shared venv for --url-list
      chunking:
        window_tokens: 8000     # = batch_token_target / 4
        overlap_tokens: 800     # = window_tokens × 0.10
        tiktoken_encoding: cl100k_base
      # --- Pass 1: schema induction -------------------------------------------
      pass1:
        batch_token_target: 32000  # conservative; MUST stay ≤ context_window − max_output_tokens
        max_workers: 4
        per_file_batching: false
        max_schema_proposals: 25  # cap raw proposals sent to harmonize_schema; prevents context overflow on large corpora
        random_seed: 0  # fixed seed for deterministic batch sampling when batches exceed max_schema_proposals; scoped per-call, not shared across repeated Pass 1 invocations
      # --- Pass 2: instance extraction ----------------------------------------
      pass2:
        max_workers: 4
        stateful_chunks: true
        prep_mode: batch_chunks             # per_file | concat | batch_chunks — controls how files are prepared before Pass 2
        per_file_token_target: 32000   # token cap per file; used when prep_mode=per_file. Files over this are truncated to the first N tokens. 0 = no cap
        concat_batch_token_target: 32000  # target token budget per virtual batch; used when prep_mode=concat
        batch_token_target: 32000         # token budget per chunk batch; used when prep_mode=batch_chunks
        batch_per_file: false              # when true, never mix chunks from different files in one batch; used when prep_mode=batch_chunks
        batch_retry_max: 1                 # max in-run retry rounds for failed batches (0 = disabled)
      normalize_names:
        enabled: true
        max_names_per_type: 50000 # safety-net cap per type; primary batch sizing is controlled by batch_token_target
        batch_token_target: 32000 # max tokens per LLM normalization call; types are bin-packed greedily up to this limit
      # --- Orphan recovery -----------------------------------------------------
      orphan_pass:
        enabled: false
        min_cooccurrence: 1
        top_k_per_orphan: 5
        confidence_base: 0.5
        confidence_weight: 0.5
        max_workers: 4
        schema_max_restarts: 0
        excerpt_window: 4000
        excerpt_context: 1500
        excerpt_max_total: 16000
        blank_recovery_enabled: true
        connected_sample_size: 200
      # --- Assembly & deduplication -------------------------------------------
      assembly:
        confidence_agg: mean
        edge_id_prefix: "edge-"
        edge_id_hex_length: 6
        edge_dedup_separator: "|"
        confidence_fallback: 0.0
        confidence_scalar_omitted: 0.5
      # --- Export --------------------------------------------------------------
      export:
        schema_namespace: "http://mykg.local/schema/"
        data_namespace: "http://mykg.local/data/"
        rdf_namespace: "http://www.w3.org/1999/02/22-rdf-syntax-ns#"
        rdfs_namespace: "http://www.w3.org/2000/01/rdf-schema#"
        schema_prefix_label: ex
        data_prefix_label: data
        comment_width: 42
        skos_namespace: "http://www.w3.org/2004/02/skos/core#"
        networkx_enabled: true
        obsidian_enabled: true
        obsidian_vault_dir: obsidian_vault
        neo4j_csv_enabled: false
        neo4j_csv_dir: neo4j_csv
      # --- Pipeline infrastructure --------------------------------------------
      error_gate:
        enabled: false
        threshold: 3
      feedback:
        max_file_chars: 224640
      paths:
        output_dir: output
        intermediate_dir: intermediate
        sessions_dir: mykg_sessions
      output:
        json_indent: 2
      logging:
        max_bytes: 10485760
        backup_count: 3
        llm_log: false
        capture_prompts: false
        error_output_max_chars: 500
      report:
        enabled: true           # generate walkthrough.md after each extract run; set false to skip
      merge_graphs:
        reextraction_strategy: surgical  # none | surgical | full — controls re-extraction when merged schema has new properties absent from a source session
        surgical_top_k_chunks_per_property: 0  # 0 = no cap; >0 = top-K chunks per new property (ranked by domain/range node co-occurrence)
        human_review: false             # pause for human review of merged schema; overridden by --human-review CLI flag
        orphan_pass_max_restarts: 1     # max schema-gap restart loops during merge orphan pass (0 = disabled)
      append:
        grow_schema_backfill_top_k_chunks_per_type: 10  # --append --grow-schema: 0 = disable back-fill; >0 = top-K old chunks re-extracted per newly added concept/property (ranked by domain/range/hierarchy node co-occurrence) (D52)
    # --- MCP server — mykg mcp-serve settings --------------------------------
    mcp:
      enabled: true                # master toggle for the MCP server (mykg mcp-serve) (mykg mcp-serve)
      host: localhost              # bind address for streamable HTTP (ignored for stdio)
      port: 3100                   # port for streamable HTTP (ignored for stdio)
      transport: stdio             # default transport: stdio | streamable_http

  # ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
  ollama-local:
  # Ollama — local inference, no API key required.
  # Requires Ollama running at base_url (default: http://localhost:11434).
  # Change llm.model to any model you have pulled (e.g. `ollama pull llama3.3`).
  #
  # Token budget chain — adjust for your model's actual context window:
  #   ① context_window    = 64000  (adjust per model; check `ollama show <model>`)
  #   ② max_output_tokens = 16000
  #   ③ input_headroom    = ① − ② = 48000
  #      batch_token_target = 16000  (doubled from 8K baseline)
  #      window_tokens      = batch_token_target / 4 = 4000
  #      overlap_tokens     = window_tokens × 0.10 = 400
  # ---------------------------------------------------------------------------
    provider: ollama
    llm_retry:
      max_retries: 2        # retries on empty/blank response; set to 0 to disable
    llm:
      context_window: 64000      # ① model's total context limit — adjust per model
      max_output_tokens: 16000    # ② output cap (doubled)
      model: gemma4:31b-cloud       # any model you have pulled via `ollama pull`
      base_url: http://localhost:11434
      timeout: 1800                # seconds per LLM call; local inference can be slow
      stream: false               # Ollama streaming — keep false for pipeline use
      retry_429_max: 3
      retry_429_base_delay: 2.0
    pipeline:
      # --- Ingestion & chunking -----------------------------------------------
      ingest:
        max_workers: 4            # local inference is CPU/GPU-bound; keep workers modest
      # --- Preprocess (optional non-md document conversion via MinerU) ----------
      # MinerU runs inside an ephemeral uv-managed venv created per parse-docs
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      preprocess:
        enabled: true                # set true to convert non-md files (PDF/DOCX/images) via MinerU before ingest
        subdir: _preprocessed         # converted .md files land in <session>/input/<subdir>/
        keep_artifacts: false        # when false (default) keep only the final <stem>.md; drop MinerU's nested subtree, images/, and .mineru.json sidecar. Set true to retain everything MinerU wrote (debugging).
        extra_args: []                # extra arguments passed through to mineru (e.g. ["--backend", "pipeline"])
        timeout_seconds: 1800         # mineru run-phase timeout (30 min)
        uv_path: uv                   # path or PATH name of the uv CLI (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter version uv pins the ephemeral venv to
        mineru_spec: "mineru[all] pdftext<0.7.0"      # package spec installed via `uv pip install -U <spec>`
        install_timeout_seconds: 1800 # install-phase timeout (30 min); first-run downloads PyTorch/Ray
        extensions:                   # suffixes the preprocess step may convert (.md is native and never listed). Backend per suffix is hardcoded: HTML → markdownify in-process, everything else → MinerU. Remove a line to skip that suffix.
          - .pdf
          - .docx
          - .doc
          - .pptx
          - .xlsx
          - .png
          - .jpg
          - .jpeg
          - .html
          - .htm
          - .txt
      # --- Fetch (optional web crawl via fetch-web command) --------------------
      # The crawler runs inside an ephemeral uv-managed venv created per fetch-web
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      fetch:
        enabled: true                 # master toggle for the fetch-web command
        output_dir: mykg_web_fetch    # default base folder for fetch-web output (./<output_dir>/<domain>/); overridden by --output
        strategy: same-domain         # link-following scope, passed to Crawlee's enqueue_links(strategy=...):
                                       #   same-domain — follow links to the same registrable domain, across subdomains (e.g. docs.example.com + www.example.com)
                                       #   same-origin — follow links matching scheme+host+port exactly (stricter than same-domain; subdomains excluded)
                                       #   all         — follow any link found, including off-site domains (use with care: max_pages/max_depth become the only guardrails)
        max_pages: 1000                # hard cap on total fetched pages (bounds runtime)
        max_depth: 1                   # max crawl depth from the seed URL
        respect_robots: true          # honor robots.txt disallow + crawl-delay
        request_delay_seconds: 0.5    # politeness delay between requests
        concurrency: 4                # max concurrent requests
        download_assets: true         # download linked binaries whose suffix is in preprocess.extensions
        timeout_seconds: 1800         # crawl run-phase timeout (30 min)
        uv_path: uv                   # uv CLI path (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter pinned for the ephemeral crawl venv
        crawlee_spec: "crawlee[beautifulsoup]"   # spec installed via `uv pip install -U`
        install_timeout_seconds: 1800 # install-phase timeout (30 min)
        github_clone_enabled: true    # master toggle for the GitHub-repo git-clone path (vs Crawlee)
        github_clone_depth: 1          # `git clone --depth N` — shallow clone (history depth only; full working tree is always cloned). 1 = latest commit's files only, no history. Increase (or set higher) only if you want .git/ history in _repo/ for provenance.
        github_clone_timeout_seconds: 1800  # git clone timeout (30 min)
        max_workers: 1                 # max concurrent seed crawls inside one shared venv for --url-list
      chunking:
        window_tokens: 4000      # = batch_token_target / 4
        overlap_tokens: 400      # = window_tokens × 0.10
        tiktoken_encoding: cl100k_base
      # --- Pass 1: schema induction -------------------------------------------
      pass1:
        batch_token_target: 16000  # doubled; MUST stay ≤ context_window − max_output_tokens
        max_workers: 1              # local GPU can usually handle a small degree of parallelism
        per_file_batching: false
        max_schema_proposals: 25  # cap raw proposals sent to harmonize_schema; prevents context overflow on large corpora
        random_seed: 0  # fixed seed for deterministic batch sampling when batches exceed max_schema_proposals; scoped per-call, not shared across repeated Pass 1 invocations
      # --- Pass 2: instance extraction ----------------------------------------
      pass2:
        max_workers: 1
        stateful_chunks: true
        prep_mode: batch_chunks                # per_file | concat | batch_chunks — controls how files are prepared before Pass 2
        per_file_token_target: 16000   # token cap per file; used when prep_mode=per_file. Files over this are truncated to the first N tokens. 0 = no cap
        concat_batch_token_target: 16000  # target token budget per virtual batch; used when prep_mode=concat
        batch_token_target: 16000         # token budget per chunk batch; used when prep_mode=batch_chunks
        batch_per_file: false              # when true, never mix chunks from different files in one batch; used when prep_mode=batch_chunks
        batch_retry_max: 1                 # max in-run retry rounds for failed batches (0 = disabled)
      normalize_names:
        enabled: true             # run the name-normalization LLM step to resolve surface-form variants before deduplication
        max_names_per_type: 500   # keep smaller — local models are slower; safety-net cap, primary control is batch_token_target
        batch_token_target: 16000 # max tokens per LLM normalization call; types are bin-packed greedily up to this limit
      # --- Orphan recovery -----------------------------------------------------
      orphan_pass:
        enabled: false
        min_cooccurrence: 1
        top_k_per_orphan: 5
        confidence_base: 0.5
        confidence_weight: 0.5
        max_workers: 4
        schema_max_restarts: 0
        excerpt_window: 2000
        excerpt_context: 500
        excerpt_max_total: 8000
        blank_recovery_enabled: true
        connected_sample_size: 50
      # --- Assembly & deduplication -------------------------------------------
      assembly:
        confidence_agg: mean
        edge_id_prefix: "edge-"
        edge_id_hex_length: 6
        edge_dedup_separator: "|"
        confidence_fallback: 0.0
        confidence_scalar_omitted: 0.5
      # --- Export --------------------------------------------------------------
      export:
        schema_namespace: "http://mykg.local/schema/"
        data_namespace: "http://mykg.local/data/"
        rdf_namespace: "http://www.w3.org/1999/02/22-rdf-syntax-ns#"
        rdfs_namespace: "http://www.w3.org/2000/01/rdf-schema#"
        schema_prefix_label: ex
        data_prefix_label: data
        comment_width: 42
        skos_namespace: "http://www.w3.org/2004/02/skos/core#"
        networkx_enabled: true
        obsidian_enabled: true
        obsidian_vault_dir: obsidian_vault
        neo4j_csv_enabled: false
        neo4j_csv_dir: neo4j_csv
      # --- Pipeline infrastructure --------------------------------------------
      error_gate:
        enabled: false
        threshold: 3
      feedback:
        max_file_chars: 32000
      paths:
        output_dir: output
        intermediate_dir: intermediate
        sessions_dir: mykg_sessions
      output:
        json_indent: 2
      logging:
        max_bytes: 10485760
        backup_count: 3
        llm_log: false
        capture_prompts: false
        error_output_max_chars: 500
      report:
        enabled: true           # generate walkthrough.md after each extract run; set false to skip
      merge_graphs:
        reextraction_strategy: surgical  # none | surgical | full — controls re-extraction when merged schema has new properties absent from a source session
        surgical_top_k_chunks_per_property: 0  # 0 = no cap; >0 = top-K chunks per new property (ranked by domain/range node co-occurrence)
        human_review: false             # pause for human review of merged schema; overridden by --human-review CLI flag
        orphan_pass_max_restarts: 1     # max schema-gap restart loops during merge orphan pass (0 = disabled)
      append:
        grow_schema_backfill_top_k_chunks_per_type: 10  # --append --grow-schema: 0 = disable back-fill; >0 = top-K old chunks re-extracted per newly added concept/property (ranked by domain/range/hierarchy node co-occurrence) (D52)
    # --- MCP server — mykg mcp-serve settings --------------------------------
    mcp:
      enabled: true                # master toggle for the MCP server (mykg mcp-serve) (mykg mcp-serve)
      host: localhost              # bind address for streamable HTTP (ignored for stdio)
      port: 3100                   # port for streamable HTTP (ignored for stdio)
      transport: stdio             # default transport: stdio | streamable_http

  # ---------------------------------------------------------------------------
  agent-claude-code:
  # Agent mode (D49) — LLM answers are produced by a Claude Code skill via a
  # filesystem inbox/outbox. The pipeline writes a task envelope to
  # intermediate/<inbox_dir>/, the skill drains the inbox using subagents, and
  # writes <id>.answer.json + <id>.done to intermediate/<outbox_dir>/.
  #
  # Install the skill once:
  #   ln -s "$(pwd)/src/mykg/data/skills/mykg" ~/.claude/skills/mykg
  # Then in Claude Code:  /mykg <input_dir>
  #
  # No API key required — the skill uses the host coding assistant's own
  # reasoning. See docs/agent-mode.md for the full contract.
  # ---------------------------------------------------------------------------
    provider: agent
    llm_retry:
      max_retries: 0
    llm:
      context_window: 200000    # agent delegates to Claude Code skill; this is the skill's effective context
      model: claude-sonnet-4-6  # informational — the skill uses whatever model the host assistant runs
      max_output_tokens: 32000  # passed through to the task envelope; the skill enforces it
      timeout: 1800             # seconds the pipeline will wait for a .done sentinel
      retry_429_max: 0          # agent provider does not hit API rate limits
      retry_429_base_delay: 2.0
    agent:
      inbox_dir: agent_inbox            # relative to session intermediate/
      outbox_dir: agent_outbox          # relative to session intermediate/
      poll_interval_seconds: 2          # how often the adapter checks for the .done sentinel
    pipeline:
      ingest:
        max_workers: 8
      preprocess:
        enabled: true
        subdir: _preprocessed
        keep_artifacts: false
        extra_args: []
        timeout_seconds: 1800
        uv_path: uv
        uv_python_version: "3.12"
        mineru_spec: "mineru[all] pdftext<0.7.0"
        install_timeout_seconds: 1800
        extensions:
          - .pdf
          - .docx
          - .doc
          - .pptx
          - .xlsx
          - .png
          - .jpg
          - .jpeg
          - .html
          - .htm
          - .txt
      # --- Fetch (optional web crawl via fetch-web command) --------------------
      # The crawler runs inside an ephemeral uv-managed venv created per fetch-web
      # call and deleted on exit; nothing is installed into mykg's interpreter.
      fetch:
        enabled: true                 # master toggle for the fetch-web command
        output_dir: mykg_web_fetch    # default base folder for fetch-web output (./<output_dir>/<domain>/); overridden by --output
        strategy: same-domain         # link-following scope, passed to Crawlee's enqueue_links(strategy=...):
                                       #   same-domain — follow links to the same registrable domain, across subdomains (e.g. docs.example.com + www.example.com)
                                       #   same-origin — follow links matching scheme+host+port exactly (stricter than same-domain; subdomains excluded)
                                       #   all         — follow any link found, including off-site domains (use with care: max_pages/max_depth become the only guardrails)
        max_pages: 1000                # hard cap on total fetched pages (bounds runtime)
        max_depth: 1                   # max crawl depth from the seed URL
        respect_robots: true          # honor robots.txt disallow + crawl-delay
        request_delay_seconds: 0.5    # politeness delay between requests
        concurrency: 4                # max concurrent requests
        download_assets: true         # download linked binaries whose suffix is in preprocess.extensions
        timeout_seconds: 1800         # crawl run-phase timeout (30 min)
        uv_path: uv                   # uv CLI path (uv is a core mykg dependency)
        uv_python_version: "3.12"    # interpreter pinned for the ephemeral crawl venv
        crawlee_spec: "crawlee[beautifulsoup]"   # spec installed via `uv pip install -U`
        install_timeout_seconds: 1800 # install-phase timeout (30 min)
        github_clone_enabled: true    # master toggle for the GitHub-repo git-clone path (vs Crawlee)
        github_clone_depth: 1          # `git clone --depth N` — shallow clone (history depth only; full working tree is always cloned). 1 = latest commit's files only, no history. Increase (or set higher) only if you want .git/ history in _repo/ for provenance.
        github_clone_timeout_seconds: 1800  # git clone timeout (30 min)
        max_workers: 2                 # max concurrent seed crawls inside one shared venv for --url-list
      chunking:
        window_tokens: 25000
        overlap_tokens: 2500
        tiktoken_encoding: cl100k_base
      pass1:
        batch_token_target: 15000
        max_workers: 2                # the skill dispatches up to this many subagents per wave
        per_file_batching: false
        max_schema_proposals: 25  # cap raw proposals sent to harmonize_schema; prevents context overflow on large corpora
        random_seed: 0  # fixed seed for deterministic batch sampling when batches exceed max_schema_proposals; scoped per-call, not shared across repeated Pass 1 invocations
      pass2:
        max_workers: 2
        stateful_chunks: true
        prep_mode: batch_chunks
        per_file_token_target: 15000   # token cap per file; used when prep_mode=per_file. Files over this are truncated to the first N tokens. 0 = no cap
        concat_batch_token_target: 15000
        batch_token_target: 15000
        batch_per_file: false
        batch_retry_max: 1
      normalize_names:
        enabled: true
        max_names_per_type: 50000 # safety-net cap per type; primary batch sizing is controlled by batch_token_target
        batch_token_target: 15000 # max tokens per LLM normalization call; types are bin-packed greedily up to this limit
      orphan_pass:
        enabled: false
        min_cooccurrence: 1
        top_k_per_orphan: 5
        confidence_base: 0.5
        confidence_weight: 0.5
        max_workers: 4
        schema_max_restarts: 0
        excerpt_window: 4000
        excerpt_context: 1500
        excerpt_max_total: 16000
        blank_recovery_enabled: true
        connected_sample_size: 200
      assembly:
        confidence_agg: mean
        edge_id_prefix: "edge-"
        edge_id_hex_length: 6
        edge_dedup_separator: "|"
        confidence_fallback: 0.0
        confidence_scalar_omitted: 0.5
      export:
        schema_namespace: "http://mykg.local/schema/"
        data_namespace: "http://mykg.local/data/"
        rdf_namespace: "http://www.w3.org/1999/02/22-rdf-syntax-ns#"
        rdfs_namespace: "http://www.w3.org/2000/01/rdf-schema#"
        schema_prefix_label: ex
        data_prefix_label: data
        comment_width: 42
        skos_namespace: "http://www.w3.org/2004/02/skos/core#"
        networkx_enabled: true
        obsidian_enabled: true
        obsidian_vault_dir: obsidian_vault
        neo4j_csv_enabled: false
        neo4j_csv_dir: neo4j_csv
      error_gate:
        enabled: false
        threshold: 3
      feedback:
        max_file_chars: 64000
      paths:
        output_dir: output
        intermediate_dir: intermediate
        sessions_dir: mykg_sessions
      output:
        json_indent: 2
      logging:
        max_bytes: 10485760
        backup_count: 3
        llm_log: false
        capture_prompts: false
        error_output_max_chars: 500
      report:
        enabled: true
      merge_graphs:
        reextraction_strategy: surgical  # none | surgical | full — controls re-extraction when merged schema has new properties absent from a source session
        surgical_top_k_chunks_per_property: 0
        human_review: false
        orphan_pass_max_restarts: 0
      append:
        grow_schema_backfill_top_k_chunks_per_type: 10  # --append --grow-schema: 0 = disable back-fill; >0 = top-K old chunks re-extracted per newly added concept/property (D52)
    # --- MCP server — mykg mcp-serve settings --------------------------------
    mcp:
      enabled: true                # master toggle for the MCP server (mykg mcp-serve) (mykg mcp-serve)
      host: localhost              # bind address for streamable HTTP (ignored for stdio)
      port: 3100                   # port for streamable HTTP (ignored for stdio)
      transport: stdio             # default transport: stdio | streamable_http
