# Copyright (C) 2026 Sebastian Ryszard Kruk (dev@kruk.me)
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published
# by the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>
#
services:
  nginx:
    # Custom build: adds the native nginx-module-otel for Layer 6 OTel instrumentation.
    # Falls back gracefully if the OTel Collector is not running (otel_trace is non-blocking).
    build:
      context: .
      dockerfile: deploy/Dockerfile.nginx
    restart: always
    ports:
      - "${NGINX_PORT:-8000}:80"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./app/static/covers:/usr/share/nginx/html/covers:ro
    depends_on:
      - web
      - frontend
  web:
    build:
      context: .
      dockerfile: deploy/Dockerfile
      args:
        APP_VERSION: ${APP_VERSION:-prod}
    restart: always
    command: >-
      sh -c "python scripts/fix_alembic.py && flask db upgrade && env PYTHONPATH=. python scripts/sync_db_permissions.py && env PYTHONPATH=. python scripts/init_auth.py && if command -v opentelemetry-instrument >/dev/null 2>&1; then exec opentelemetry-instrument gunicorn --bind 0.0.0.0:5000 --access-logfile - --error-logfile - --log-level info run:app; else exec gunicorn --bind 0.0.0.0:5000 --access-logfile - --error-logfile - --log-level info run:app; fi"
    env_file:
      - ${ENV_FILE:-.env}
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - ADMIN_EMAIL=${ADMIN_EMAIL}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - OTEL_SERVICE_NAME=iqoqo-api
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318
      - OTEL_TRACES_EXPORTER=${OTEL_TRACES_EXPORTER:-none}
      - OTEL_METRICS_EXPORTER=${OTEL_METRICS_EXPORTER:-none}
      - OTEL_LOGS_EXPORTER=${OTEL_LOGS_EXPORTER:-none}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./exports:/usr/src/app/exports
      - ./app/static/covers:/usr/src/app/app/static/covers
      - ./app/static/gallery:/usr/src/app/app/static/gallery
      - ./app/static/uploads/raw_covers:/usr/src/app/app/static/uploads/raw_covers
      - ./scripts:/usr/src/app/scripts
      - ./shared:/usr/src/app/shared
      - ./.allegro_token.json:/usr/src/app/.allegro_token.json
      - ${HOME}/.config/rclone:/home/appuser/.config/rclone:ro
    ports:
      - "127.0.0.1:${WEB_PORT:-5000}:5000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        NEXT_PUBLIC_API_URL: /api
        APP_VERSION: ${APP_VERSION:-prod}
        NEXT_PUBLIC_APP_VERSION: ${APP_VERSION:-prod}
        NEXT_PUBLIC_FRONTEND_URL: ${NEXT_PUBLIC_FRONTEND_URL:-https://iqoqo.cc}
    env_file:
      - ${ENV_FILE:-.env}
    environment:
      - FLASK_API_URL=${FLASK_API_URL:-http://web:5000/api}
      - NEXT_PUBLIC_APP_VERSION=${APP_VERSION:-prod}
      - NEXT_PUBLIC_FRONTEND_URL=${NEXT_PUBLIC_FRONTEND_URL:-https://iqoqo.cc}
      - NEXT_PUBLIC_APP_URL=${NEXT_PUBLIC_APP_URL:-https://iqoqo.cc}
      - APP_URL=${NEXT_PUBLIC_APP_URL:-https://iqoqo.cc}
      - OTEL_SERVICE_NAME=iqoqo-frontend
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318
      - OTEL_TRACES_EXPORTER=${OTEL_TRACES_EXPORTER:-none}
      - OTEL_METRICS_EXPORTER=${OTEL_METRICS_EXPORTER:-none}
      - OTEL_LOGS_EXPORTER=${OTEL_LOGS_EXPORTER:-none}
    restart: always
    ports:
      - "127.0.0.1:${FRONTEND_PORT:-3000}:3000"
    depends_on:
      - web
    extra_hosts:
      - "host.docker.internal:host-gateway"

  db:
    image: postgres:18-alpine
    restart: always
    ports:
      - "127.0.0.1:${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-iqoqo}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
      - POSTGRES_DB=${POSTGRES_DB:-iqoqo}
      - PGDATA=/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-iqoqo} -d ${POSTGRES_DB:-iqoqo}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  redis:
    image: redis:8-alpine
    restart: always
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    ports:
      - "127.0.0.1:${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  worker:
    build:
      context: .
      dockerfile: deploy/Dockerfile
      args:
        APP_VERSION: ${APP_VERSION:-prod}
    restart: always
    command: >-
      sh -c "if command -v opentelemetry-instrument >/dev/null 2>&1; then exec opentelemetry-instrument celery -A app.core.celery_app:celery worker --loglevel=info --pool=${CELERY_POOL:-threads} --concurrency=${CELERY_CONCURRENCY:-4}; else exec celery -A app.core.celery_app:celery worker --loglevel=info --pool=${CELERY_POOL:-threads} --concurrency=${CELERY_CONCURRENCY:-4}; fi"
    env_file:
      - ${ENV_FILE:-.env}
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - ADMIN_EMAIL=${ADMIN_EMAIL}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - OTEL_SERVICE_NAME=iqoqo-celery-worker
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318
      - OTEL_TRACES_EXPORTER=${OTEL_TRACES_EXPORTER:-none}
      - OTEL_METRICS_EXPORTER=${OTEL_METRICS_EXPORTER:-none}
      - OTEL_LOGS_EXPORTER=${OTEL_LOGS_EXPORTER:-none}
      - OTEL_PYTHON_LOGGING_AUTO_INSTRUMENTATION_ENABLED=${OTEL_PYTHON_LOGGING_AUTO_INSTRUMENTATION_ENABLED:-false}
    volumes:
      - ./exports:/usr/src/app/exports
      - ./app/static/covers:/usr/src/app/app/static/covers
      - ./app/static/gallery:/usr/src/app/app/static/gallery
      - ./app/static/uploads/raw_covers:/usr/src/app/app/static/uploads/raw_covers
      - ./shared:/usr/src/app/shared
      - ./.allegro_token.json:/usr/src/app/.allegro_token.json
      - ${HOME}/.config/rclone:/home/appuser/.config/rclone:ro
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"
volumes:
  postgres_data:
